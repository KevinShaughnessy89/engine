// #pragma once

// template <typename Key, typename Signature>  // generalized
// class FunctionDispatcher;

// template <typename Key, typename ReturnType,
//           typename... Args>  // Signature -> ReturnType(Args...) -> ex void(const Shape*)
// class FunctionDispatcher<Key, ReturnType(Args...)> {}
